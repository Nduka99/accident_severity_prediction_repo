version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/model_artifacts:/app/backend/model_artifacts
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
